const InitState={
  isAuth:false,
  isLoading:false,
  isError:false,
  token:""
}
const reducer = (store=InitState,action) => {
  switch(action){

    default :
    return store
  }
  // return store
};

export  {reducer};
